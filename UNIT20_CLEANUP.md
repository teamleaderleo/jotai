# Unit 20 cleanup checklist

Temporary execution-carrier record.

After exact-head receipts are transferred to the Fieldwork packet:

- delete `.github/workflows/unit20-direct-source.yml`;
- delete every root `UNIT20_*.md` file;
- verify the resulting branch contains only the production source and two target-native tests;
- run or retain a check proving the temporary files are absent on the later clean head;
- obtain complete-diff review on that later clean head.
