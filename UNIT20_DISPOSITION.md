# Unit 20 disposition

Temporary owned-fork record for the direct-source execution carrier.

Disposition before execution: `EXECUTE`.

Clearing condition: focused Node 22/24/26 tests, changed-file lint/format/type checks, and build pass on the exact source branch. After receipt transfer, remove this file, the other `UNIT20_*.md` files, and `.github/workflows/unit20-direct-source.yml`; then review the later clean source head.

The final upstream route and any combination with the async generation successor remain separate decisions. Public upstream contact is unauthorized.
